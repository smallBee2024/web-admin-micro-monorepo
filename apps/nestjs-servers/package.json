{
  "name": "wujie-demo-nestjs-server",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal NestJS server for examples/servers",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "ts-node-dev --respawn --transpile-only src/main.ts",
    "start1": "node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^10.3.0",
    "@nestjs/devtools-integration": "^0.2.1",
    "@nestjs/mapped-types": "*",
    "@nestjs/platform-express": "^10.3.0",
    "@nestjs/platform-fastify": "^10.3.0",
    "@nestjs/typeorm": "^11.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "html-to-docx": "^1.8.0",
    "mysql2": "^3.15.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "typeorm": "^0.3.27"
  },
  "devDependencies": {
    "@types/node": "^20.11.30"
  },
  "engines": {
    "node": ">=18"
  }
}
